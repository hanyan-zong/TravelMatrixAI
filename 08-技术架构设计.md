# 技术架构设计

## 1. 非技术理解版

系统可以理解成 7 个部分：

1. 网页前台：运营人员看到的操作界面。
2. 业务后台：处理产品、素材、内容、账号、发布任务。
3. 数据库：保存所有结构化数据。
4. 文件仓库：保存图片、视频、音频和导出内容包。
5. AI 服务：负责写文案、做审核、生成脚本、生成封面建议。
6. 视频处理服务：负责字幕、配音、剪辑、转码。
7. 发布连接器：负责对接不同平台或生成半自动发布任务。

## 2. 推荐技术选型

### 2.1 前端

- Next.js
- React
- Tailwind CSS
- shadcn/ui
- Tiptap 富文本编辑器
- FullCalendar 发布日历

### 2.2 后端

二选一：

- NestJS：适合 TypeScript 全栈团队。
- FastAPI：适合 Python 和 AI 工程团队。

如果后续 AI 和视频处理较重，建议后端主服务用 NestJS，AI/视频服务用 Python 独立服务。

### 2.3 数据库和存储

- PostgreSQL：主数据库。
- Redis：缓存和任务队列。
- S3 或 MinIO：图片、视频、音频、导出包。
- Elasticsearch 或 Meilisearch：后续用于素材和内容搜索。

### 2.4 后台任务

- BullMQ：Node.js 任务队列。
- Celery：Python 任务队列。
- Temporal：更复杂时用于长流程编排。

第一版用 BullMQ 或 Celery 即可。

### 2.5 视频处理

- FFmpeg：剪辑、转码、加字幕、合成音频。
- Remotion：模板化生成视频。
- Whisper 或平台语音识别：字幕校对。

## 3. 服务拆分

第一版可以是单体应用加后台任务，不要过早拆太多服务。

建议结构：

- Web App：前端页面。
- API Server：业务接口。
- Worker：后台任务。
- AI Gateway：统一调用不同 AI 模型。
- Media Worker：视频和图片处理。

后期再拆：

- Publishing Service
- Analytics Service
- Compliance Service
- Asset Search Service

## 4. 核心流程

### 4.1 内容生成流程

1. 用户选择旅游产品。
2. 用户选择平台和账号。
3. API Server 读取产品、品牌、账号定位。
4. AI Gateway 生成平台内容。
5. Compliance Service 做初步审核。
6. 内容保存为草稿。
7. 前端展示可编辑版本。

### 4.2 视频生成流程

1. 用户选择内容草稿。
2. AI 生成视频脚本。
3. AI 生成分镜。
4. 系统从素材库匹配素材。
5. 用户确认分镜。
6. Media Worker 生成配音、字幕、封面。
7. FFmpeg 或 Remotion 渲染视频。
8. 输出多个平台比例。

### 4.3 发布流程

1. 内容审核通过。
2. 用户加入发布日历。
3. 到时间后检查平台发布方式。
4. 支持 API 的平台自动发布。
5. 不支持的平台生成发布包并提醒人工发布。
6. 发布完成后记录链接和状态。

## 5. 安全设计

必须包含：

- 用户登录
- 角色权限
- 操作日志
- 平台 token 加密保存
- 文件访问权限
- AI 请求日志脱敏
- 发布前权限检查
- 高风险内容禁止自动发布

## 6. 部署建议

第一版：

- Docker Compose
- 一台云服务器
- PostgreSQL
- Redis
- MinIO
- API + Worker

正式商用：

- 独立数据库服务器
- 对象存储
- CDN
- 日志监控
- 自动备份
- 多 worker 横向扩容

