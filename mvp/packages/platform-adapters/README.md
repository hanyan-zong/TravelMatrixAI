# Platform Adapters

平台连接器把不同平台的发布规则隔离在独立模块里。

当前 MVP 只实现 `ManualAssistAdapter`：

- 验证标题、正文、素材数量。
- 不执行自动发布。
- 生成半自动发布包。

后续可以为微博、今日头条、企业号等平台新增 API adapter，但必须继续遵守：

- 不绕验证码。
- 不绕登录风控。
- 不做刷互动。
- 不做自动注册或养号。
- 没有稳定官方能力的平台默认人工确认。
