# 项目目录与工程规范

## 1. 推荐仓库结构

```text
travelmatrix-ai/
  apps/
    web/                 # 前端网页
    api/                 # 后端 API
    worker/              # 后台任务
  packages/
    shared/              # 前后端共享类型
    prompts/             # AI 提示词模板
    platform-adapters/   # 平台发布连接器
  services/
    media-worker/        # 视频、图片、音频处理
    ai-gateway/          # AI 模型统一调用
  infra/
    docker-compose.yml
    postgres/
    minio/
  docs/
    product/
    technical/
  scripts/
```

## 2. 第一版可以简化

如果团队人数少，第一版可以先这样：

```text
travelmatrix-ai/
  web/
  api/
  worker/
  docs/
  docker-compose.yml
  .env.example
```

不要一开始拆太多服务。先把业务流程跑通。

## 3. 命名规范

页面命名：

- `dashboard`
- `travel-products`
- `media-assets`
- `content-studio`
- `video-studio`
- `review-center`
- `publish-calendar`
- `analytics`

核心对象命名：

- `TravelProduct`
- `MediaAsset`
- `PersonAsset`
- `SocialAccount`
- `PostDraft`
- `PostVariant`
- `VideoProject`
- `PublishTask`
- `ComplianceCheck`
- `PlatformMetric`

## 4. 状态规范

内容状态：

- `draft`
- `pending_review`
- `approved`
- `rejected`
- `scheduled`
- `published`
- `failed`

风险等级：

- `low`
- `medium`
- `high`

发布方式：

- `api`
- `enterprise_api`
- `manual_assist`

平台枚举：

- `xiaohongshu`
- `douyin`
- `wechat_channels`
- `toutiao`
- `weibo`
- `douban`

## 5. 环境变量建议

```text
DATABASE_URL=
REDIS_URL=
S3_ENDPOINT=
S3_BUCKET=
S3_ACCESS_KEY=
S3_SECRET_KEY=
OPENAI_API_KEY=
AI_PROVIDER=
JWT_SECRET=
APP_BASE_URL=
```

## 6. 开发规范

- 所有发布操作必须写日志。
- 所有 AI 生成结果必须保存原始输入和输出。
- 所有高风险内容必须阻止自动发布。
- 所有素材必须有授权状态。
- 平台连接器必须统一接口。
- 不同平台的文案生成逻辑必须分开。
- 不允许把验证码绕过、刷互动、养号逻辑写入系统。

## 7. 测试规范

第一版至少测试：

- 产品录入
- 素材上传
- 多平台内容生成
- 审核阻断
- 发布日历
- 发布包导出
- 数据录入
- 权限限制

