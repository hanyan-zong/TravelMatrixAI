# 下次继续开发交接

## 1. 当前项目位置

主文档：

```text
C:\Users\001\Documents\Codex\TravelMatrixAI
```

MVP 工程：

```text
C:\Users\001\Documents\Codex\TravelMatrixAI\mvp
```

Codex skill：

```text
C:\Users\001\.codex\skills\travel-matrix-ai-planner
```

## 2. 今天已完成

### 2.1 设计文档

已完成 00 到 19 的 Markdown 文档，覆盖：

- 产品总览
- 功能模块
- 页面流程
- 多模态 AI 内容工厂
- 发布与合规
- 开发路线图
- PRD
- 技术架构
- 数据模型
- AI 提示词
- MVP 任务清单
- 接口草案
- 风险验收
- 工程规范
- 数据库 SQL 草案
- 平台连接器规范
- AI 任务配置
- MVP 工程交付说明
- 下次继续开发交接

### 2.2 MVP 前端

位置：

```text
C:\Users\001\Documents\Codex\TravelMatrixAI\mvp\web
```

技术：

- React
- TypeScript
- Vite
- lucide-react

已实现：

- 运营工作台 UI
- 左侧导航
- 产品库视图
- 素材库视图
- 内容工厂视图
- 发布日历视图
- 数据看板视图
- 当前用户显示
- 选择旅游产品
- 调用后端生成多平台内容
- 接入发布任务和数据看板 API
- 接口离线兜底

验证：

```powershell
cd C:\Users\001\Documents\Codex\TravelMatrixAI\mvp\web
npm.cmd run build
```

结果：通过。

### 2.3 MVP 后端

位置：

```text
C:\Users\001\Documents\Codex\TravelMatrixAI\mvp\api
```

技术：

- FastAPI
- Pydantic
- Uvicorn
- httpx TestClient

已实现 API：

- `GET /api/health`
- `POST /api/auth/login`
- `GET /api/auth/me`
- `GET /api/travel-products`
- `POST /api/ai/generate-posts`
- `GET /api/media-assets`
- `POST /api/media-assets`
- `POST /api/ai/compliance-check`
- `GET /api/publish-tasks/calendar`
- `POST /api/publish-tasks`
- `POST /api/publish-tasks/{task_id}/result`
- `POST /api/metrics`
- `GET /api/analytics/overview`

验证：

```powershell
cd C:\Users\001\Documents\Codex\TravelMatrixAI\mvp\api
.\.venv\Scripts\python.exe check_api.py
```

结果：`API checks passed`

### 2.4 共享包

位置：

```text
C:\Users\001\Documents\Codex\TravelMatrixAI\mvp\packages
```

已实现：

- AI 提示词模板：`packages\prompts\prompts.json`
- 平台连接器类型：`packages\platform-adapters\src\types.ts`
- 人工发布适配器：`packages\platform-adapters\src\manual-adapter.ts`

### 2.5 基础设施

位置：

```text
C:\Users\001\Documents\Codex\TravelMatrixAI\mvp\infra
```

已实现：

- PostgreSQL docker compose
- Redis docker compose
- MinIO docker compose
- PostgreSQL init SQL

## 3. 当前启动方式

启动后端：

```powershell
cd C:\Users\001\Documents\Codex\TravelMatrixAI\mvp
.\start-api.ps1
```

启动前端：

```powershell
cd C:\Users\001\Documents\Codex\TravelMatrixAI\mvp
.\start-web-static.ps1
```

访问：

```text
http://127.0.0.1:4181
```

后端健康检查：

```text
http://127.0.0.1:8000/api/health
```

## 4. 当前注意事项

- Vite dev 模式在当前 Windows 权限环境下可能触发父目录扫描权限问题，所以稳定预览使用 `start-web-static.ps1`。
- 当前后端是 MVP 内存数据 + `api/data/state.json` 运行态存储。
- `api/data/state.json`、`web/dist`、`web/node_modules`、`api/.venv` 已加入 `.gitignore`。
- 当前认证是演示 token，不可用于生产。
- 当前 AI 生成是规则模拟，不是真实模型调用。
- 当前平台发布只做人工发布适配器，不做绕平台风控的自动化。

## 5. 下次优先深度优化

建议按这个顺序继续：

1. 接 PostgreSQL ORM，把当前内存模型迁移到数据库。
2. 做正式登录、密码哈希、JWT、角色权限。
3. 增加内容草稿编辑保存表和前端编辑器。
4. 做审核中心的真实队列、通过、驳回和日志。
5. 做发布包导出：标题、正文、标签、素材、审核记录。
6. 做素材上传到本地或 MinIO。
7. 做 AI Gateway，接真实模型并保留提示词版本。
8. 做视频工厂第一版：脚本、分镜、字幕、封面，不急着自动剪辑。
9. 增加 PostgreSQL migration 和 seed 数据。
10. 增加前端表单校验、错误提示和 loading 状态。

## 6. 下次开始时建议先跑

```powershell
cd C:\Users\001\Documents\Codex\TravelMatrixAI\mvp\api
.\.venv\Scripts\python.exe check_api.py
```

```powershell
cd C:\Users\001\Documents\Codex\TravelMatrixAI\mvp\web
npm.cmd run build
```

确认这两项通过后，再继续重构或新增功能。

