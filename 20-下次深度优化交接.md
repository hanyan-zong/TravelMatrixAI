# 下次深度优化交接

## 当前状态

今天已把 TravelMatrix AI MVP 从静态演示推进到可操作闭环：

- 内容工厂：生成多平台内容、单条保存草稿、批量保存草稿。
- 视频工厂：生成视频 / 图片 / 软文创意包。
- IP 口播智能体：IP 人设、爆款结构、提词器、镜头计划。
- 爆款拆解：输入参考口播文本，拆解结构并原创改写。
- 批量口播：一次生成多条不同角度口播，并保存草稿。
- 素材库：素材登记、授权状态、人物同意、使用范围、来源、归属方、素材绑定草稿。
- 审核中心：草稿送审、通过、驳回、审核记录。
- 发布包：生成发布包、平台适配、素材清单、运营确认清单、人工确认记录。
- 发布日历：从确认后的发布包创建发布任务，回填发布链接和首日数据。
- 数据看板：读取指标汇总并给出复盘建议。

## 当前运行入口

```text
C:\Users\001\Documents\Codex\TravelMatrixAI\mvp
```

API：

```powershell
cd C:\Users\001\Documents\Codex\TravelMatrixAI\mvp
.\start-api.ps1
```

Web：

```powershell
cd C:\Users\001\Documents\Codex\TravelMatrixAI\mvp
.\start-web-static.ps1
```

浏览器：

```text
http://127.0.0.1:4181
```

如果浏览器显示旧页面，使用 `Ctrl + F5` 强制刷新。

## 验证命令

```powershell
cd C:\Users\001\Documents\Codex\TravelMatrixAI\mvp\api
.\.venv\Scripts\python.exe check_api.py
```

```powershell
cd C:\Users\001\Documents\Codex\TravelMatrixAI\mvp\web
npm.cmd run build
```

预期结果：

- `API checks passed`
- Vite build succeeds

## 建议的下一轮深度优化

1. 数据持久化升级：从内存 + `state.json` 迁移到 PostgreSQL ORM，并补 migration。
2. 前端拆分：当前 `main.tsx` 已过大，应拆成 `apiClient`、模块组件、类型文件和页面组件。
3. 真实编辑能力：草稿、口播、发布包都应支持编辑保存，而不仅是生成和确认。
4. 发布任务细化：增加账号矩阵、排期冲突检测、发布状态时间线、失败原因。
5. 素材治理升级：增加文件上传、本地/MinIO 存储、过期提醒、授权附件。
6. AI Gateway：接入真实模型调用，保存 prompt 版本、输入、输出、成本和失败日志。
7. 权限系统：替换 demo auth，加入角色、工作区、JWT、密码哈希。
8. 自动化测试：补前端交互测试，覆盖内容生成、保存、送审、发布包、发布任务回填。
9. UX 优化：把“下一步”做成明确流程向导，减少用户不知道点哪里的问题。
10. 合规强化：对价格、签证、安全、人物授权、声音克隆、数字人使用增加强制检查。

## 重要约束

- 不做账号农场、批量注册、验证码绕过、风控绕过、刷量、伪评论。
- 平台发布优先采用官方 API、企业接口或人工确认发布包。
- 真人、数字人、声音克隆、人物图片必须保留明确授权记录。
- 爆款拆解只能学习结构和节奏，不能复制他人原文、画面、声音或专属人设。
