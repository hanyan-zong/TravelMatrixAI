# 平台连接器规范

## 1. 目标

每个平台的规则不同，所以系统不能把发布逻辑写死在业务代码里。应该为每个平台建立独立连接器。

连接器负责：

- 验证内容格式
- 判断是否支持自动发布
- 执行 API 发布或生成半自动发布包
- 返回发布结果
- 拉取或录入数据

## 2. 统一接口

```ts
export interface PublisherAdapter {
  platform: Platform;
  capabilities(): PlatformCapabilities;
  validate(post: PlatformPost): Promise<ValidationResult>;
  publish(post: PlatformPost): Promise<PublishResult>;
  schedule?(post: PlatformPost, scheduledAt: Date): Promise<ScheduleResult>;
  exportManualPackage(post: PlatformPost): Promise<ManualPackage>;
  fetchMetrics?(remotePostId: string): Promise<PlatformMetrics>;
}
```

## 3. 能力声明

```ts
export interface PlatformCapabilities {
  supportsApiPublish: boolean;
  supportsSchedule: boolean;
  supportsMetricsFetch: boolean;
  supportsVideo: boolean;
  supportsImages: boolean;
  maxTextLength?: number;
  maxImages?: number;
  allowedRatios?: string[];
  defaultPublishMethod: 'api' | 'enterprise_api' | 'manual_assist';
}
```

## 4. 发布结果

```ts
export interface PublishResult {
  success: boolean;
  remotePostId?: string;
  publishedUrl?: string;
  status: 'published' | 'failed' | 'manual_required';
  message?: string;
  rawResponse?: unknown;
}
```

## 5. 平台默认策略

### 小红书

默认：

- `manual_assist`

原因：

- 普通账号自动发布稳定性和合规性不足。
- 第一版应生成图文、标签、封面、发布说明，由人工确认。

### 抖音

默认：

- `manual_assist`
- 企业能力可用时再切换 `enterprise_api`

### 微信视频号

默认：

- `manual_assist`

### 今日头条

默认：

- 优先评估开放平台
- 不可用时 `manual_assist`

### 微博

默认：

- 优先评估开放平台
- 可作为较早 API 接入平台

### 豆瓣

默认：

- `manual_assist`
- 内容辅助优先

## 6. 连接器禁止行为

连接器不允许实现：

- 绕验证码
- 绕登录风控
- 自动注册账号
- 自动刷赞评论收藏
- 伪造设备指纹
- 采集用户隐私数据

## 7. 发布包内容

半自动发布包应包含：

- 平台名称
- 账号名称
- 标题
- 正文
- 标签
- 图片或视频文件
- 封面
- 发布时间
- 注意事项
- 审核记录

